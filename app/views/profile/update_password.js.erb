<% if @user.errors.any? %>
$('.form-error-messages').hide().html("<%= j(render 'shared/error_messages', resource: @user) %>").fadeIn('slow');
$('.btn-progress').button('reset');
<% else %>
Turbolinks.visit('/profile');
<% end %>