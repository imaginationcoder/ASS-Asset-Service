- flash.each do |msg_type, message|
  .alert.alert-dismissible.fade.in{role: 'alert',class: bootstrap_class_for(msg_type)}
    %button.close{"aria-label" => "Close", "data-dismiss" => "alert", type: 'button'}
      %span{"aria-hidden" => "true"} &times;
    = message
